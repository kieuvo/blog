import * as React from 'react';

export interface IBlogCategoryProps {
}

export default function BlogCategory(props: IBlogCategoryProps) {
    return (
        <div>
            <h1>Blog category</h1>
        </div>
    );
}
